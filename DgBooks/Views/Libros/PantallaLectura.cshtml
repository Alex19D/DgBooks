@model DgBooksDB.Libro

@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Open+Sans:wght@400;600&display=swap"
          rel="stylesheet">
    <link href="~/css/home.css" rel="stylesheet" />
    <link href="~/css/pantallla.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-BmbxuPwQa2lc/FVzBcNJ7UAyJxM6wuqIj61tLrc4wSX0szH/Ev+nYRRuWlolflfl" crossorigin="anonymous">
    <title>DgBooks</title>
</head>
<body>
    <header>
        <div class="nLogo">
            <a id="logo" href="#home"><img src="~/Images/Logo1.png" alt="Logo Image" /></a>
        </div>
        <nav class="main-nav">
            <a href="@Url.Action("PantallaDetalles", new { id=Html.DisplayFor(model=>model.intIdLibor), idU=ViewBag.User.intIdUsuario})">Home</a>
            <a href="#generos">Generos</a>
            <a href="#fav">Favoritos</a>

        </nav>
        <nav class="sub-nav">
            <a href="#"><i class="fas fa-search sub-nav-logo"></i></a>
            <a href="#"><i class="fas fa-bell sub-nav-logo"></i></a>
            <a href="@Url.Action("EditarUsuario", "Usuario", new { id=ViewBag.user.intIdUsuario})">
                <i class="fas fa-user"></i>  @ViewBag.user.strNombreUsuario
            </a>
            <a href="@Url.Action("index", "Home")"><i class="fas fa-sign-out-alt"></i></a>
        </nav>

    </header>

    <br />
    <br />
    <br />

    <main>

        <embed src="@Html.DisplayFor(model => model.strLinkLibro)" type="application/pdf" height="900" width="2100" />

    </main>

    <script src="https://kit.fontawesome.com/2c36e9b7b1.js" crossorigin="anonymous"></script>
</body>
</html>